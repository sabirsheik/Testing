# Leave MONGODB_URI empty to run the API in local memory mode.
MONGODB_URI=mongodb://127.0.0.1:27017
MONGODB_DB=northstar
PORT=4000
# Set this when the frontend and API run on different origins. Leave empty to use the Vite proxy.
VITE_API_URL=
